import { Page, Locator } from '@playwright/test';

/**
 * Webhook Component
 * Handles: webhook trigger selection and configuration
 */
export class WebhookComponent {
    private readonly page: Page;

    constructor(page: Page) {
        this.page = page;
    }

    // TODO: Add locators and methods
    // Webhook option in trigger listbox
}
