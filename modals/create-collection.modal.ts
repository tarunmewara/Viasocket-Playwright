import { Page, Locator } from '@playwright/test';

/**
 * Create Collection Modal
 * Handles: collection name input, suggestion chips, create/submit button
 */
export class CreateCollectionModal {
    private readonly page: Page;

    constructor(page: Page) {
        this.page = page;
    }

    // TODO: Add locators and methods
    // Name of Collection textbox, suggestion chips, Create Collection button
}
